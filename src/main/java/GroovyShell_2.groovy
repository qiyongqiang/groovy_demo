class GroovyShell_2 {
    public String sayHello(String name, String sex, int age) {
        println('GroovyShell_2 的sayHello(Sting name,String sex,int age)方法');
        return "name: " + name + ",sex:" + sex + ",age:" + age;
    }
}
